"""Learner-written unit test contracts for phase 1."""

# TODO: write a unit test that the query parser accepts name, start, and end.
# It should convert URL query-string values into the internal booking input shape.

# TODO: write a unit test that missing query parameters are rejected.
# It should cover at least a missing name and a missing end time.

# TODO: write a unit test for the interval overlap predicate.
# It should prove partial overlaps are rejected and back-to-back intervals are allowed.

# TODO: write a unit test that booking rows serialize to JSON-compatible dictionaries.
# Datetimes should become ISO-8601 strings with timezone offsets.
