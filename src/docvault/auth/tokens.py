"""JWT token helpers.

Creates and validates short-lived bearer tokens for authenticated DocVault
requests.
"""

# TODO: import datetime and jwt.

# TODO: implement create_access_token(subject: str) -> str.
# Encodes a JWT with sub, iat, and exp claims using DOCVAULT_JWT_SECRET.

# TODO: implement decode_access_token(token: str) -> dict.
# Validates signature and expiry; invalid or expired tokens must raise.
